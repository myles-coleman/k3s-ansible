apiVersion: v1
kind: Service
metadata:
  name: nixos-mcp
  namespace: nixos-mcp
spec:
  selector:
    app: nixos-mcp
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
