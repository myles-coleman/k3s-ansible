apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: discord-bot
  namespace: argocd
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: discord-bot
  project: default
  source:
    path: manifests/cluster/discord-bot
    repoURL: https://github.com/myles-coleman/k3s-ansible
    targetRevision: main
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 2m
      limit: 5
