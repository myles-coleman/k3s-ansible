apiVersion: v1
kind: Service
metadata:
  name: {{APP_NAME}}-external{{SSL_SUFFIX}}
  namespace: {{APP_NAME}}
spec:
  type: ExternalName
  externalName: "{{EXTERNAL_IP}}"
  ports:
    - port: {{PORT}}
      targetPort: {{PORT}}
