apiVersion: v1
kind: Service
metadata:
  name: {{APP_NAME}}
  namespace: {{APP_NAME}}
spec:
  selector:
    app: {{APP_NAME}}
  ports:
    - name: http
      protocol: TCP
      port: {{PORT}}
      targetPort: {{TARGET_PORT}}
