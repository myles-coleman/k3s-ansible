apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{APP_NAME}}-config
  namespace: {{APP_NAME}}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{STORAGE_SIZE}}
  storageClassName: longhorn
